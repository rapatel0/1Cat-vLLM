# Forge 001: Maximize Hy3 64K MTP decode on V100

## Metadata

```forge-metadata
forge_id: 001
title: Maximize Hy3 64K MTP decode on V100
status: in_progress
kind: initial
requested_effort: high
effective_effort: high
parent: -
dependencies: -
target: /Users/ravi/repos/1Cat-vLLM
write_set: ["docs/forge/ledger.tsv", "docs/forge/FORGE-001.md", "vllm/v1/attention/ops/triton_unified_attention.py", "vllm/v1/attention/backends/triton_attn.py", "tests/kernels/attention/test_triton_unified_attention.py", "tests/kernels/attention/test_sm70_triton_mtp_segmented_attention.py", "benchmarks/benchmark_hy3_64k_segmented_attention.py", "docs/benchmarking/forge_001_hy3_64k_results.md", "vllm/v1/attention/backends/flash_attn_v100.py", "flash-attention-v100/flash_attn_v100/flash_attn_interface.py", "flash-attention-v100/kernel/flash_decode_paged.cu", "flash-attention-v100/kernel/flash_decode_turboquant.cu", "flash-attention-v100/kernel/fused_mha_api.cpp", "flash-attention-v100/include/fused_mha.h", "tests/kernels/attention/test_sm70_flash_v100_mtp_smallq_exactness.py", "tests/kernels/attention/test_sm70_flash_v100_decode_planner.py", "tests/v1/attention/test_sm70_flash_v100_policy.py", "vllm/v1/attention/backends/turboquant_attn.py", "vllm/v1/attention/ops/triton_turboquant_decode.py", "vllm/config/attention.py", "tests/quantization/test_turboquant.py", "benchmarks/benchmark_sm70_turboquant_quality.py"]
risk: High — SM70 attention dispatch, numerical order, CUDA-graph capture, native kernels, and maximum-context serving are affected; invalid kernels can silently corrupt generation or crash an eight-GPU process, and the V100 node is currently fully reserved by a live Hy3 deployment. Code changes are locally reversible, but no service interruption, local-registry publication, or live promotion is permitted without separately explicit authority and a verified restore plan.
created_at: 2026-07-19T00:26:13Z
updated_at: 2026-07-19T00:28:16Z
plan_hash: 2da505623fe53592352b32353954320f47c2f5f702e110380c493ddf1780e37e
escalation_state: clear
terminal_hash: -
work_commit: -
```
**Execution pressure — binding:** This item authorizes delivery of the requested outcome through the full affected application now. `increment` records chronology only; it does not mean partial, provisional, narrow, or lower-pressure execution. Do not return after planning, scaffolding, a first patch, one subsystem, narrow test success, one failed route, a checkpoint, or a progress summary. Continue through integration, criterion-by-criterion validation, adversarial attack, repair, regression testing, and owned commits while any safe in-scope action remains. Incompleteness is not a terminal result. Before any incomplete return, perform the next safe concrete action if one exists. Yield incomplete only when the runtime actually prevents another action, an explicit hard ceiling is reached, the user pauses, or a genuine authority, safety, capability, or external block remains.

**Campaign mandate:** Restore and maximize end-to-end Hy3 speculative decode at a genuinely occupied 65,536-token context on eight V100-SXM2 GPUs. Treat the verified MTP multi-query attention dispatch cliff—not context length itself—as the initial defect, but do not preordain the winning mechanism. The promoted candidate must be the fastest fully validated route found across segmented Triton, Flash-V100 grouped decode, TurboQuant KV, speculation scheduling, and launch/graph integration; it must clear the quantitative 64K, batching, short-context, numerical, deterministic, profiler, and reproducibility gates below. Microbench-only wins, repeated-token-only wins, backend labels without instruction/counter proof, queued “batch” requests, or faster incorrect output do not count. Use one coordinator plus up to three concurrent workers, at least three mechanism-diverse rounds and twelve concrete missions across at least five families, with independent falsification in every round and two candidate red-team passes; these are an effort floor, not a completion ceiling. Keep canonical Forge state in the coordinator worktree and isolate concurrent writers in dedicated worktrees based on the latest verified Forge commit. The eight-V100 node is currently fully reserved by `llm/onecat-vllm-hy3-sm70`; do not disturb it, benchmark inside it, publish a candidate image, or claim canary authority unless the user separately and explicitly approves the exact interruption, immutable-image, snapshot, restore, and known-answer procedure below. No push, PR, live-service promotion, or destructive cluster action is authorized. Renew the campaign while any exact criterion is unmet and a safe discriminating action remains.

## Seed request

The user asked: “Lets attack this problem with forge lifecycle. I'm happy to explore all optimizations available to get this working. Create the intial forge prompt for review.”

The controlling problem is the observed Hy3 maximum-context result of approximately 1.919 output tok/s for one slot and 3.809 aggregate tok/s for two slots on eight V100s, despite decode being expected to be bandwidth-oriented and despite a retained short-context MTP result of 13.2286 tok/s for one slot and 25.8151 aggregate for two slots. The user’s historical anchors were approximately 5 tok/s/slot before MTP and approximately 12 tok/s/slot with MTP, and they explicitly reject treating sub-baseline noise as progress. The user wants the fastest overall system with maximum context and optimized batching, not an explanation that normalizes 1.9 tok/s.

This initial item is deliberately recorded as `planned` for user review. Do not cross the planning boundary or begin implementation until the user explicitly continues or resumes this Forge item.

## Inherited continuity capsule with provenance

- Sources read:
  - Current repository branch `experiment/bonsai-q2-volta-mma` at `be1011dde0e6ff381be965c129dead79d02b2b8f`, plus the dirty-worktree snapshot captured on 2026-07-18; this is the actual shared source environment and contains unrelated Bonsai/GDN/MoE/model-runner work that must be preserved. The tracked attention subtree is unchanged from `hy3-sm70-marlin@66232f91a`, so a clean worktree at the current HEAD retains both that Hy3 attention state and later tracked history.
  - `vllm/v1/attention/ops/triton_unified_attention.py:874-895,1030-1085,1160-1180`; these lines define `BLOCK_Q`, suppress segmented 3D attention for `max_seqlen_q > 1`, choose the 2D/3D grid, choose prefill versus decode warps, and perform segmented reduction.
  - `vllm/v1/attention/backends/triton_attn.py:55-57,143-218`; these lines fix sixteen softmax segments, derive the 3D threshold, and allocate scratch.
  - Live model config for `cyankiwi/Hy3-AWQ-INT4` snapshot `c8b08e2c23dd45cb1b277d1290800e40c3dd8eec`: 80 layers, 64 query heads, 8 KV heads, head dimension 128, 192 experts, top-8 routing, FP16 KV. TP8 therefore presents 8 query heads and 1 KV head per GPU.
  - `/Users/ravi/repos/homelab/manifests/onecat-vllm-hy3-sm70/20-deployment.yaml:60-79`; the current service is TP8/PP1, maximum length 65,536, maximum sequences 2, Triton target/draft attention, and native MTP with two speculative tokens.
  - Read-only Kubernetes state on 2026-07-18: `llm/onecat-vllm-hy3-sm70` currently reserves all eight GPUs on `gpu-01`, using image `localhost:32000/onecat-vllm:1cat1.2.1-v100`. Earlier idle-capacity authority is stale; there is no free V100 for an interference-free experiment.
  - `docs/benchmarking/hy3_64k_experiment_ledger.md`; retained exact-context results are 1.919 tok/s single slot and 3.809 aggregate at two slots, with 100% acceptance on the repeated-`the` control.
  - Retained r11 artifacts under `/workspace/bench-hy3/r11-tp8-bench`; the short-context MTP1 results are 13.2286 tok/s single and 25.8151 aggregate at concurrency two.
  - Historical DCGM samples for the scored windows: the 64K two-slot run averaged 3.94% memory-copy utilization, 74.69% GPU utilization, and 79.36 W; the fast short-context two-slot run averaged 55.46%, 97.58%, and 156.23 W. No retained Hy3 Nsight trace exists.
  - `flash-attention-v100/flash_attn_v100/flash_attn_interface.py:259-280` and the retained TurboQuant run: Flash-V100 TurboQuant decode with 32 splits cannot cover 65,536 tokens when its largest partition is 1,024.
- Still-valid decisions:
  - Maximum-context performance means decoding after a genuinely resident 64K prompt, not merely launching a 64K-capable server with a short prompt.
  - Batch tests must use only simultaneously resident maximum-context requests. FP16 KV currently supports two; batch four/eight is invalid unless a candidate first proves enough real KV capacity. Retained TurboQuant capacity evidence is approximately 202,496 cache tokens, sufficient for only three 64K requests; it does not authorize batch four.
  - TP4×PP2 is pruned absent materially new leverage because retained attempts were slower and its prototype runner performs per-step host materialization; TP8/PP1 remains the comparison topology.
  - Q2/Bonsai, Q4-KV for other models, unrelated GDN work, and the live eight-replica service are outside this item.
- Still-valid assumptions:
  - The causal explanation “multi-query verifier dispatch under-occupies the GPU” is a strong inference, not yet a per-kernel profiler fact: MTP2 has query length 3; GQA gives `BLOCK_Q=2`; `max_seqlen_q > 1` forces 2D; one slot therefore has about two active CTAs per target layer on an 80-SM V100, and two slots approximately double that count.
  - The 3D kernel appears structurally capable of handling multiple query blocks, but correctness, scratch sizing, causal masking, CUDA-graph replay, and performance for q-length 2/3 remain unproved.
- Relevant artifacts and verification:
  - `benchmarks/benchmark_sm70_triton_attention_3d.py` already models 64K, TP8-local 8Q/1KV/D128 and can compare current 2D with forced 3D for multi-token query lengths. It is pre-existing untracked user work at blob `f74201f4f55d8c51917f82210a529615d60c4e98`, has no saved result, is read-only evidence for this item, and must not be silently adopted or committed. Build the owned unified harness at `benchmarks/benchmark_hy3_64k_segmented_attention.py`.
  - Static byte accounting gives 2.5 GiB of target KV per full slot per GPU and about 5 GiB of target-attention reads per MTP2 verifier iteration. The observed DCGM utilization disproves an HBM-saturated explanation but does not predict end-to-end throughput.
- Unresolved gaps inherited:
  - No measured kernel-duration, CTA/occupancy, achieved-bandwidth, or instruction profile for the actual q=3 verifier.
  - No q>1 segmented numerical or graph-capture test.
  - No matched 64K comparison of corrected Triton, Flash-V100 small-query, valid TurboQuant, and speculation depths under realistic text.
  - The exact attainable maximum-context ceiling is unknown.
- Supersessions already in force:
  - Supersede “1.9 tok/s is an expected consequence of 64K KV bandwidth” with “the path is demonstrably not saturating HBM and has a code-level under-parallelized dispatch cliff”; evidence is the source gate, launch geometry, near-linear batch scaling, and DCGM counters.
  - Supersede the previous decode-warp A/B as a target-verifier experiment: q=3 selects the prefill schedule, so changing only the decode-warp knob did not materially exercise the 80-layer hot path.

## Current task statement and controlling definitions

Deliver a locally committed, profiler-proven, numerically correct Hy3 SM70 maximum-context decode implementation and an isolated eight-V100 canary configuration that selects the fastest validated candidate.

- **Maximum context:** exactly 65,536 model tokens of allocated context, with the scored steady-decode region beginning from a prompt of 64,000-64,001 tokens and continuing for at least 256 generated tokens without truncation, eviction, queuing, or lowering the configured limit.
- **Slot:** one independently scheduled request with its own decode stream. Shared-prefix testing must be labeled separately from independent-prefix testing.
- **Steady decode rate:** post-first-token output tokens divided by the shared wall-clock interval, using streamed usage accounting; report per-slot and aggregate. Also retain standard `vllm bench serve` output throughput for the fixed short regression control, without mixing the metrics.
- **MTP2 verifier:** a target-model verification forward with query length `num_speculative_tokens + 1 = 3`; all three causal query positions and acceptance accounting are in scope.
- **Working attention fix:** the real integrated q=3 target path exposes enough context-axis parallelism to remove the two-CTA cliff, is visible in profiler evidence, and clears every correctness and performance gate. Merely forcing a flag in a synthetic caller is not a fix.
- **Maximally fast variant:** the Pareto-best candidate on integrated realistic 64K single-slot and two-slot aggregate throughput after the required mechanism portfolio and effort floor, provided it clears every hard gate. A route that clears a minimum threshold but loses to another fully validated route is not selected. After the apparent winner is frozen, two independent final search waves must find no candidate improving either primary point by more than the greater of 3% or three times the measured relative MAD without regressing the other.
- **Known fact:** the active 64K run is not HBM-saturated; retained DCGM memory-copy utilization is approximately 3.94%.
- **Inference to test:** q>1’s forced 2D dispatch is the dominant attention cause. Treat it as the leading hypothesis, not guaranteed truth.
- **Source hierarchy:** observed repository/runtime state and reproducible profiler/test evidence outrank historical prose; historical artifacts establish baselines but not current dispatch.

The full affected application includes kernel dispatch, scratch/workspace sizing, causal masks, graph capture/replay, backend policy, speculative scheduling integration, KV representation, benchmark accounting, deterministic output, and isolated serving. Ordinary implementation choices are autonomous. Changing the model, quantized weights, maximum context, tokenizer, prompt semantics, or comparison topology is not an optimization of this target and requires an amendment.

## Exact completion contract

1. Reproduce and preserve the current exact-shape baselines with source/config hashes and warm-state evidence: independently resident 64K MTP2 batch one and simultaneous batch two, plus the historical r11-style 128-input/128-output MTP1 batch one/two control. Freeze model revision, TP/PP, FP16 KV unless KV format is the declared candidate, graph/scheduler settings, argv/env, runtime/native hashes, GPU clocks/power state, and prefix-cache policy. Alternate baseline and candidate on the same GPUs; record at least five scored steady runs per integrated configuration, median, MAD/range, and a paired confidence interval. No JIT compilation may occur in a scored interval, and dirty bind-mounted source cannot supply terminal candidate evidence.
2. Produce an Nsight Compute, Nsight Systems, or equivalently direct CUDA-profiler trace for the actual integrated q=3 verifier showing the pre-fix and candidate kernel names, grids/CTA counts, per-kernel duration, achieved memory throughput, active/eligible warps, and Tensor Core or relevant instruction mix. DCGM alone is insufficient.
3. Remove the q>1 dispatch cliff in the selected implementation. At 64K MTP2 batch one, each target layer must use context-axis splitting with at least 32 active attention CTAs per GPU, or an independently justified launch that achieves equal-or-better utilization and throughput. Scratch/workspace capacity and dispatch must be derived from actual query-token rows such as `q.shape[0]`, not merely `num_seqs`; insufficient scratch must cause a proven-correct 2D fallback rather than overflow. The trace must prove the integrated service selects the corrected route; no silent 2D fallback is allowed in the scored shape.
4. Validate attention numerics against an independent FP32 reference and the current 2D implementation for query lengths 1, 2, 3, and 5; batches 1 and 2; context lengths 1, 15, 16, 17, 4,096, 32,768, 64,001, 65,521, and 65,536; ragged causal position boundaries; partial final blocks; multiple seeds and adversarial finite values; randomized and noncontiguous paged block tables; graph-padded and mixed live/padded requests; preemption/reorder metadata; shared and independent prefixes. Candidate FP16 error must be no worse than the current implementation’s FP32-reference envelope, with no NaN/Inf, causal leakage, race, out-of-bounds access, or unwritten scratch rows. Native CUDA candidates also require a focused compute-sanitizer/memcheck pass.
5. Validate CUDA-graph capture and replay for all selected target/draft batch shapes, particularly q-token shapes 3 and 6 for MTP2. Repeated eager and graph outputs must meet the same reference envelope, use correctly sized stable scratch, and run without inference-time JIT, allocation growth, race, or stale-segment contamination.
6. Preserve model behavior: deterministic greedy known-answer output must match a frozen target/no-spec token sequence at short, 4K, 32K, and 64K contexts and at concurrency one/two; eager and graph output must agree; a realistic 64K long-context needle retrieval suite and at least two independent realistic 64K prompts must pass before and after. Report MTP acceptance by position and length. Repeated `the` remains only a continuity/upper-acceptance control and cannot be the sole quality or performance workload.
7. Achieve at least 4.0× the retained 1.919 tok/s single-slot exact-context baseline (at least 7.676 tok/s) on the continuity control and at least 3.5× the retained 3.809 aggregate two-slot baseline (at least 13.332 tok/s). Both gains must exceed the greater of 20%, five times baseline relative MAD, or a paired 95% confidence-interval lower bound of 10%; neither primary point may be traded away. The selected candidate must also improve the median realistic two-slot 64K workload by at least 2.0× over its matched pre-change baseline.
8. Preserve short-context and ordinary decode capability: on the exact r11 MTP1 128/128 control, median single-slot throughput must be at least 12.831 tok/s and two-slot aggregate at least 25.041 tok/s (no more than 3% below 13.2286/25.8151). Current MTP2 short decode, q-length-1/no-spec decode, and representative 4K/32K/64K prefill/TTFT must regress by no more than 3% beyond measured noise. If environment drift prevents reproducing the historical baseline before code changes, freeze a matched baseline and require no more than 3% candidate regression while explicitly retaining the historical numbers as a non-equivalent reference.
9. Preserve maximum-context batching. FP16-KV batch two must run concurrently rather than queue, both slots must clear the performance gate independently, and the slower slot must remain within 5% of the faster slot. Do not require ideal C2/C1 scaling after a candidate actually saturates a shared resource; score C1 and C2 independently. TurboQuant batch three may be measured only after server metrics prove three independently occupied 65,536-token caches fit. Batch four/eight results count only after the backend proves physical capacity for four/eight simultaneous 65,536-token requests and server metrics show all are concurrently running; otherwise they are prohibited as misleading experiments.
10. Exercise and compare the ranked route portfolio with concrete artifacts and matched discriminators. Start with narrowly gated q=2–4 segmented Triton at 16/32/64 segments and select the segmented/decode schedule by `use_3d`; use progressive elimination rather than a Cartesian tile/warp search. Then test a q=3/GQA8 fusion that loads each KV tile once for all three query positions and eight query heads, profiling register pressure, spills, occupancy, long-scoreboard stalls, and reduction cost. Continue to an exact SM70 D128 grouped-GQA/XQA Flash-V100 kernel only if its exact-shape microbenchmark can beat the winning Triton route. Make TurboQuant cover 64K with at least 64 partitions or safe fallback before performance work, then consider only a fused GQA8/q3 packed-cache route with quality validation. Re-evaluate no speculation, MTP1/q=2, and MTP2/q=3 only after attention fusion; MTP4+ remains pruned absent a new acceptance mechanism. Optimize graph/launch/reduction overhead only after direct profiles show it is material. Prune a family only with measured falsification or a precise correctness/capability blocker and reopen it only on materially new evidence. After the initial portfolio converges, run two fresh independent search/falsification waves; completion requires that neither produces a fully gated candidate exceeding the stability threshold in the definition of “maximally fast.”
11. Select the fastest fully gated candidate, integrate it without changing the model or context contract, run the independent red-team and regression suite, and—only after separately explicit service-disruption and local-image-publication authority—execute one isolated single-service eight-V100 canary. Before mutation, snapshot the live deployment’s spec, generation, replicas, image digest, argv, env, selector, and known-answer behavior. Use an immutable `forge-001-<full-source-sha>-<config-hash>` local-registry tag, uniquely labeled Forge resources, no live traffic, and exact-target probes before creation. The canary must report exact source commit and image digest, native-extension hashes, launch configuration, context/slot residency, acceptance length/rate, per-slot/aggregate throughput, DCGM counters, profiler evidence, deterministic/needle results, and exact rollback command. On failure, remove only exact Forge-labeled resources, restore the matching captured deployment state, and rerun its known-answer check. A one-GPU microkernel job is not an integrated Hy3 canary.
12. Leave a reproducible artifact set in the owned paths: implementation and tests, unified benchmark harness, updated experiment ledger with rejected-route evidence, exact build/test/benchmark commands, selected configuration, profiler artifact locations/checksums, and isolated local commits. `git diff --check`, relevant lint, unit/kernel tests, build, deterministic tests, and end-to-end gates must all pass.

## Insufficient results and non-goals

- A microbenchmark speedup without the integrated 64K service selecting the same kernel is insufficient.
- A backend log line, high generic GPU utilization, DCGM alone, or theoretical roofline is not proof of corrected dispatch.
- Repeated-`the` performance or 100% MTP acceptance alone is insufficient; realistic independent prompts and needle correctness are mandatory.
- A result obtained by shortening the resident prompt, lowering `max_model_len`, changing the model/checkpoint, omitting FP16-KV comparison, silently queuing requests, or counting prompt tokens is invalid.
- Short-context sweeps are prohibited except the fixed regression control and small unit-test boundaries. Candidate ranking must use 64K-relevant shapes.
- Batch four/eight at FP16 KV is invalid under the current 2.41-slot physical capacity. It becomes valid only after a measured KV-capacity change.
- Re-running TP4×PP2 before repairing its per-step host materialization, decode-only warp sweeps that do not touch q=3, MTP4+, broad scalar Flash q=3 sweeps, Flash-TurboQuant with only 32 splits, or speculation depths already falsified is prohibited absent a new mechanism and explicit reopen evidence.
- Cartesian segment/tile/warp sweeps are prohibited; use a profiler-guided progressive elimination sequence on exact 64K q=2/3 shapes.
- A numerically faster implementation with worse FP32-reference error, nondeterministic greedy output, needle failure, graph-only corruption, or reduced acceptance is not promotable.
- A Triton-only implementation is not preselected. Conversely, native CUDA work is not justified merely because it is lower level.
- Do not modify unrelated Bonsai/Q2/GDN/MoE/model-runner code, quantized model weights, tokenizer assets, the live deployment without the separately explicit disruption clause, or other repositories.
- No push, PR, full-service promotion, destructive cluster cleanup, or fabricated benchmark/profiler evidence is in scope.

## Execution protocol and phase checkpoints

1. **Baseline freeze and causal proof:** establish a dedicated clean Hy3 integration worktree at the recorded current HEAD `be1011dde0e6ff381be965c129dead79d02b2b8f`—whose tracked attention subtree matches `hy3-sm70-marlin@66232f91a`—and transfer only reviewed Forge state. Record the dirty-root porcelain/index snapshot and hashes of relevant pre-existing untracked files, but do not adopt them. Finish the owned unified exact-shape harness and capture source/config/native hashes. After the user separately authorizes the required service interruption, rerun alternating warm matched controls and collect direct q=3 kernel profiles plus DCGM. Gate: the launch geometry and time attribution explain the retained symptom or the registry is revised before implementation changes.
2. **Correctness harness:** add forced q>1 segmented attention reference tests, scratch sentinels, graph replay, paged-table adversaries, and q-length/context boundaries. Gate: the harness fails the unsafe or unsupported route and distinguishes current 2D from candidate dispatch without relying on performance.
3. **Mechanism-diverse implementation rounds:** assign isolated workers to segmented Triton, Flash-V100 D128 grouped decode, TurboQuant 64K planning/quality, and MTP/graph integration. Every worker returns a patch/commit, exact benchmark/profile evidence, numerical result, remaining gap, and integration note. Gate: registry statuses are evidence-backed and every round contains independent falsification.
4. **Cross-pollination and optimization:** transfer only concrete winning components—such as segment planner, grouped GQA reuse, reduction strategy, or graph-safe scratch—across families; sweep only 64K-relevant q=2/3 batch 1/2 shapes. Gate: each retained component improves the integrated candidate or closes a correctness gap.
5. **Candidate freeze and adversarial audit:** freeze the fastest gated candidate and its claims; have independent workers attack causal masks, tail blocks, graph replay, shared-prefix accounting, acceptance behavior, dispatch fallback, build provenance, and throughput methodology. Repair and rerun affected plus regression gates.
6. **Integrated canary and closeout:** after all pre-canary gates and separately explicit disruption/publication authority, snapshot the current service, build from a clean source commit in the verified localpool workspace, publish an immutable local-registry image, run one uniquely labeled isolated eight-V100 service canary without live traffic, restore and KAT-check the prior service state, validate all criteria, inspect the owned diff/commit series, record evidence and rollback, then close only on exact success.

- Approach architecture: High dynamic portfolio with one coordinator, mechanism-isolated implementation workers, an evidence-backed approach registry, per-round falsification, cross-pollination of concrete components, candidate freeze, and independent red team.
- Application impact sweep: Kernel wrapper and planner, scratch metadata, graph capture, Flash-V100 native/Python bindings, TurboQuant split planning, speculative verifier integration, tests, benchmark accounting, and documentation are in scope. API schemas, tokenizer/model weights, unrelated model families, and live deployment manifests are materially unaffected and excluded.
- Autonomous decision policy: Use current source, profiler evidence, and matched benchmarks to make local implementation choices. Ask only if a route requires changing the model/quality contract, expanding beyond the write set, mutating the live service, transferring code/data externally, or performing a destructive/production action.
- Effort floor and hard ceilings: One coordinator plus up to three concurrent workers; at least three substantive rounds, twelve concrete missions, five mechanism families, one independent falsification mission per round, and two independent candidate red-team passes. The currently occupied eight-V100 node is the required hardware scale anchor once separately authorized and safely isolated. No user hard ceiling was specified; these floors renew while completion is unmet and safe work remains. Product runtime limits may force an `in_progress` checkpoint but are not completion.
- Free-running execution loop: hypothesis -> exact-shape artifact -> profile/test -> falsify -> repair -> matched retest -> registry synthesis -> dynamically allocate the next highest-information mission. Revisit a route only when a changed patch, transferred component, new counterexample, or stronger discriminator materially changes the evidence.
- Documentation boundary: Update canonical Forge state only at baseline freeze, substantive round synthesis, scope/authority amendment, candidate freeze, forced interruption, or terminal closeout. Raw profiles and worker notes remain outside `docs/forge`; record only paths, checksums, conclusions, and commands canonically.
- Progress commit cadence: Commit a coherent Forge-owned implementation/test or benchmark artifact after its relevant correctness validation passes. Use explicit owned paths only; never commit transient profiles, known-failing candidates, or pre-existing unrelated hunks.
- Checkpoint cadence: Baseline/profiler freeze, each material portfolio synthesis, candidate freeze, canary result, interruption, or changed authority. Do not checkpoint merely to narrate an individual experiment.
- Coordinator ownership: The coordinator alone writes `docs/forge/FORGE-001.md` and `docs/forge/ledger.tsv`, integrates candidates, owns the primary worktree, and decides promotion/termination.
- Worker isolation topology: The current dirty Bonsai worktree remains the canonical-state/orchestration worktree and is never an implementation integration surface. Before execution, designate a clean coordinator integration worktree/branch at `be1011dde0e6ff381be965c129dead79d02b2b8f`, transfer only the reviewed canonical Forge record, and record its path/base hash. Read-only auditors may share repository context. Every concurrent writer uses another dedicated `forge/001-*` worktree/branch at the latest verified Hy3 Forge progress commit and never edits canonical Forge state. Workers return a local commit or patch, commands/results, exact gap, and integration note. The coordinator integrates selected changes and validates the combined candidate in the clean Hy3 integration worktree.
- Development environments: Use the existing local repository for orchestration and verified localpool-backed `/workspace/1Cat-vLLM` for SM70 builds. The existing eight-V100 node has no free GPU; use it only after explicit disruption authority and only through uniquely labeled, interference-free Forge jobs or the final canary. Use unique output directories and resource names per route and the existing model/cache snapshot. Do not provision new remote infrastructure or transfer code/data outside authorized local resources.
- Dirty-worktree boundary: The starting worktree contains unrelated modified and untracked Bonsai/GDN/PP/MoE/model-runner work. `benchmarks/benchmark_sm70_triton_attention_3d.py`, `docs/benchmarking/hy3_64k_experiment_ledger.md`, `benchmarks/benchmark_hy3_mtp_moe.py`, and `tests/kernels/test_hy3_mtp_moe_config.py` are pre-existing user state with starting blobs `f74201f4f55d8c51917f82210a529615d60c4e98`, `e5ca47b1c557fe55e17526ae10f149ca321cac68`, `37edde3176363d6ec38b27125cf5d3ce7709e074`, and `cb1e03ddc1a28c66b31eba671ad3a940382d96e5`, respectively. Preserve them, but do not modify, stage, transfer, or commit them. All dirty paths are read-only and excluded. Snapshot the index and porcelain state before execution and integration. If an implementation needs an excluded dirty path such as `vllm/envs.py`, `gpu_model_runner.py`, `fused_moe.py`, or `hy_v3_mtp.py`, use a clean isolated worktree, prove the hunk is independent, and amend the effective write set before editing; otherwise block rather than contaminate user work. Stage and commit only explicit owned paths, then verify the commit path set and that the original index is unchanged.
- Idempotency/rollback: Hash source, native extensions, manifests/configs, and result artifacts before each run. Derive resource and result IDs from `FORGE-001`, full work SHA, and configuration hash; probe exact names, labels, and digests before creation and reuse only an exact prior success. Store raw runs outside `docs/forge`, for example `/workspace/bench-hy3/forge-001/<sha>/<config-hash>/`, with a manifest containing source/patch/image/native hashes, command/env, driver/CUDA, GPU clocks, prompt/query/cache shapes, slot/MTP settings, warmups, and repetitions. A candidate is locally reversible by reverting its isolated commits. Before any authorized live mutation, record a matching deployment snapshot; restore only from that snapshot and clean up only exact Forge-labeled resources. Never reuse or overwrite scored artifacts.

## Adversarial validation and repair loop

- Acceptance trace:
  - Criteria 1, 7, 8, and 9 -> unified harness JSON plus raw streamed responses, server metrics, five-run medians/dispersion, and matched baseline/candidate tables.
  - Criteria 2 and 3 -> direct profiler reports with kernel/grid/counter exports and source/config hashes.
  - Criteria 4 and 5 -> targeted Pytest/kernel test commands, FP32-reference error tables, scratch sentinels, eager/graph replay, and dispatch assertions.
  - Criterion 6 -> deterministic known-answer hashes, needle outputs/scores, and realistic-prompt acceptance/throughput records.
  - Criterion 10 -> normalized approach registry with concrete artifact, evidence, exact gap, status, discriminator, and reopen condition for every family.
  - Criteria 11 and 12 -> clean-build log, isolated canary evidence bundle, owned Git diff/commit verification, lint/test/build commands, and final ledger.
- Failure catalog: incorrect causal masking for later verifier tokens; segment-tail omission/duplication; `q.shape[0]` scratch overflow; stale partials across graph replay; wrong capture-size dispatch; shared-prefix benchmark contamination; GQA KV rereads disguised by more CTAs; FP16 reduction drift changing greedy acceptance; Flash-V100 D128/XQA unsupported specialization; TurboQuant 32-split 64K failure; invalid batch concurrency; JIT/cold-cache timing; wrong prefill/decode schedule; backend fallback; native-extension/source mismatch; NCCL or MoE time misattributed to attention; DCGM percentage mistaken for kernel bandwidth; and user dirty changes entering a commit. The strongest polished-but-wrong outcome is a forced synthetic benchmark that reports a segmented speedup while the production CUDA graph still dispatches 2D and the claimed “64K” serving result actually uses a short or shared resident prefix; the route assertion, independent-prefix residency evidence, and integrated trace must jointly reject it.
- Round-level falsification: Every substantive round assigns an independent worker or context-separated pass to attack one active route with a concrete counterexample, profiler contradiction, numerical adversary, graph replay failure, or integrated benchmark. The coordinator records the resulting registry change and repair/reopen condition.
- Independent pass: Freeze candidate commit, config, profiler claims, result artifacts, and completion trace. Two independent auditors separately attack correctness/graphs and performance/provenance/methodology without being shown favored-route implementation notes beyond the frozen claims.
- Repair loop: Failure -> causal localization -> minimal patch in an isolated worktree -> rerun the failing oracle -> rerun numerics/graphs -> rerun matched 64K and short regression as affected. Another cycle is justified only by a materially changed artifact, evidence set, assumption, or discriminator.
- Promotion gate: No missing criterion, unresolved high-severity red-team finding, unexplained fallback, source/native mismatch, or unsupported performance claim. The selected candidate must be both the fastest fully gated route and above every hard threshold.

## Tools, permissions, budget, and external-action boundaries

- Available local capabilities: Git and worktrees; shell/read-only Kubernetes inspection; up to three concurrent subagents plus the coordinator; an eight-V100-SXM2-32GB node currently occupied by the Hy3 service; localpool-backed `/workspace/1Cat-vLLM`; CUDA 12.8/Torch 2.10/vLLM 1.2.1 environment; Triton, Flash-V100, DCGM/Prometheus history, existing model/cache, and repository benchmark/test infrastructure. Nsight availability must be verified before promising a specific profiler; use an equivalent direct CUDA profiler only if it supplies the required counters.
- Local authority after this planned item is explicitly continued: Read/write the target repository within the effective write set, create dedicated local branches/worktrees, run non-cluster local validation, inspect cluster state read-only, build in the existing verified workspace without external publication, and make isolated local Forge commits after validation. Use `.venv/bin/python` or `uv` for every local Python command; never use system Python or bare pip.
- External actions forbidden without separately explicit authority: scaling, replacing, or otherwise interrupting `llm/onecat-vllm-hy3-sm70`; scheduling GPU jobs while it owns the node; publishing any image to the local registry; creating the integrated canary; push; PR; live promotion; unrelated infrastructure; external code/data transfer; metered resources; destructive cleanup; credential discovery; messages; purchases; or unrelated repositories. Approval to continue ordinary Forge implementation is not by itself this disruption/publication authority. If that authority is granted, it is limited to the exact snapshot, immutable-image, uniquely labeled job/canary, no-live-traffic, cleanup, restore, and KAT procedure in criterion 11; Services, Ingress, PVCs, model weights, and selectors remain unchanged.
- Evidence-contamination boundary: Background kernel literature and upstream implementation patterns are allowed. Do not copy an exact-target benchmark result and present it as local evidence; every completion claim requires a locally reproduced artifact tied to source/config hashes. Worker prose is not evidence without its artifact.
- Budget: The stated mission/round/family/red-team floors are mandatory and separate from completion. No cost, token, or elapsed-time hard ceiling was specified. Renew model-chosen execution envelopes while exact completion is unmet and safe work remains; record only actual work and durable timestamps.
- Orchestration binding: Use the verified collaboration system literally with one coordinator and at most three simultaneous workers. Maintain the functional Forge roles of registry, dynamic allocation, isolated writers, synthesis, per-round falsification, and red team.
- Degradation: If fewer worker slots are temporarily available, preserve the same cumulative mission floor with sequential context-separated passes and a resumable backlog. Do not fabricate concurrency or silently reduce the portfolio. While the exact eight-GPU environment remains occupied or unauthorized, continue only safe local correctness/build/read-only work, then checkpoint the exact external resume condition rather than benchmarking inside the live service or substituting irrelevant hardware.

## Stop, block, escalation, and resume conditions

- Success: Every completion criterion passes with observed evidence; the fastest fully gated candidate survives both independent audits; the isolated canary succeeds; owned commits and reproducibility artifacts are verified; outcome/continuity are recorded.
- Continue: Any criterion is unmet and a safe in-scope test, profile, implementation, falsification, repair, or integration action remains. Perform it instead of returning. Completion of the effort floor triggers synthesis and renewal, not return.
- Forced interruption while remaining in_progress: An actual runtime boundary, explicit hard ceiling, or user pause. Record verified artifacts, profiler/test results, registry/pruning evidence, exact unmet criteria, next missions, and precise resume action; leave the same item `in_progress`.
- Block only when: Correctness requires a material architecture/quality choice unavailable from evidence; an implementation requires excluded dirty-path changes that cannot be isolated; required profiler/GPU/build capability is externally unavailable after safe alternatives; all safe local work is exhausted and the separately explicit service-disruption/local-publication authority remains absent or is revoked; or another safety/external boundary prevents further discriminating work.
- Low-to-High escalation: Not applicable because the item begins High. Any scope, authority, risk, write-set, or completion-contract change requires a complete numbered amendment and passed plan audit before continuing.
- Resume: Run Forge recovery/checks, re-read this item and latest checkpoint/registry, inspect actual branch/HEAD/dirty and cluster state, verify source/native/result hashes, probe whether uniquely named jobs/artifacts already completed, and continue from the earliest unverified criterion without replaying side effects.
- Hard-ceiling exhaustion: Only an explicit user/system ceiling forces interruption. Leave the item `in_progress` with the strongest verified candidate, exact remaining gap, smallest resume action, and no claim of completion.

## Plan amendments

No amendments.

## Execution log and checkpoints

### C001 — local portfolio freeze before V100 validation

- Completed phase: Three mechanism-diverse implementation/falsification rounds, the fail-closed local correctness and benchmark harness, and local portfolio synthesis. The effort floor was exceeded without selecting a winner from static evidence.
- Current repository state: Immediately before this checkpoint commit, the canonical orchestration worktree was on `experiment/bonsai-q2-volta-mma` at `9d4b1078eee26633bf35dd0426bdcc58cc67cf69`; the checkpoint adds only this canonical document. The pre-checkpoint index tree was `d60903c47b39d9edf868d5cbc1c9c59febd693cf` with no staged unrelated entry; the four recorded pre-existing untracked artifacts still hash to `f74201f4f55d8c51917f82210a529615d60c4e98`, `e5ca47b1c557fe55e17526ae10f149ca321cac68`, `37edde3176363d6ec38b27125cf5d3ce7709e074`, and `cb1e03ddc1a28c66b31eba671ad3a940382d96e5`. The clean coordinator implementation worktree is `/Users/ravi/repos/1Cat-vLLM-forge-001`, branch `forge/001-integration`, at `0c952141360b2014b69139a126bd14b1af77accd`. Native and TurboQuant candidates remain isolated and clean at `842ff44220bdde333c26ff51f7307d756c1c9537` and `351f1bf9591f63921ce2cb4b54dd00aa8e49f686`, respectively. Nothing was pushed.
- Commands and results: `SKIP=attention-backend-docs uvx pre-commit run --files <five segmented candidate files>` passed every non-skipped hook, including Ruff, mypy, SPDX, forbidden-import, forbidden-`torch.cuda`, configuration, and suggestion checks; the excluded backend-doc hook regenerates a pre-existing `FLASH_ATTN_V100` row outside this item's write set, so that unrelated hunk was removed. `/Users/ravi/repos/1Cat-vLLM/.venv/bin/python -m pytest --confcutdir=tests/kernels/attention tests/kernels/attention/test_sm70_triton_mtp_segmented_attention.py -q` reported `19 passed, 27 skipped`; the skips are CUDA-only on the local Mac. `compileall`, `git diff --check`, benchmark parser/help, source-provenance smoke, and the independent FP32 reference smoke passed. TurboQuant planner tests reported `9 passed, 1 skipped`, its focused configuration/graph-policy tests reported `5 passed`, its broader quantization tests reported `109 passed, 17 skipped`, and its exact-file pre-commit suite passed. The native candidate passed focused pre-commit/static checks, Python compilation, changed-line clang-format dry-run, SPDX/forbidden-CUDA hooks, and two independent source audits; no local CUDA toolkit or SM70 device exists, so it was deliberately not represented as compiled or numerically executed. Read-only cluster inspection on 2026-07-18 found the healthy `llm/onecat-vllm-hy3-sm70` pod at image `localhost:32000/onecat-vllm:1cat1.2.1-v100`, TP8/PP1, 65,536 maximum length, two maximum sequences, and MTP2. It requests all eight V100s; each showed approximately 31,050/32,768 MiB resident. The serving image has Torch 2.10.0+cu128/CUDA 12.8 but no `ncu` or `nsys`; the documented CUDA-devel profiler-pod route remains available only after isolation authority.
- Files changed: The segmented branch owns `vllm/v1/attention/ops/triton_unified_attention.py`, `vllm/v1/attention/backends/triton_attn.py`, `tests/kernels/attention/test_triton_unified_attention.py`, `tests/kernels/attention/test_sm70_triton_mtp_segmented_attention.py`, and `benchmarks/benchmark_hy3_64k_segmented_attention.py`. The native branch owns `flash-attention-v100/kernel/flash_decode_paged.cu`, `flash-attention-v100/include/fused_mha.h`, `flash-attention-v100/kernel/fused_mha_api.cpp`, `flash-attention-v100/flash_attn_v100/flash_attn_interface.py`, and `tests/kernels/attention/test_sm70_flash_v100_mtp_smallq_exactness.py`. The TurboQuant branch owns `vllm/config/attention.py`, `vllm/v1/attention/backends/turboquant_attn.py`, `flash-attention-v100/flash_attn_v100/flash_attn_interface.py`, `tests/quantization/test_turboquant.py`, and `tests/kernels/attention/test_sm70_flash_v100_decode_planner.py`.
- Side effects or external identifiers: Three local worktrees/branches and their local commits exist. Pillow 10.4.0 and uvloop 0.22.1 were added to the existing local test virtual environment through `uv`; no repository file changed as a result. Cluster operations were read-only. No GPU job, profiler, registry publication, image, Kubernetes resource, service interruption, push, PR, or canary was created.
- Validation already satisfied: The segmented candidate now has a narrow exact-SM70 q=2-4 production gate, flattened query-token scratch sizing, deterministic unsupported-shape fallback, causal empty-segment repair, adversarial paged-reference coverage, and q3/q6 graph-replay oracles. The native route has a standalone exact q3/D128/GQA8 one-pass-KV design, graph-stable caller-owned workspaces, 47,664-byte dynamic shared-memory bound, device-side sequence-capacity guarding, and hard-fail missing-symbol coverage. The TurboQuant route fails 32-split 64K planning closed, provides a 64-split/1,024-token plan, reuses fixed workspace, forwards the configured split count through every production continuation call, preserves q=1 graph support, and explicitly rejects unsafe q>1 capture. These are local portions of criteria 3-5, 10, and 12 only; none supplies the required SM70 runtime, profiler, integrated performance, quality, or canary evidence.
- Next action: Obtain explicit authority to snapshot and temporarily scale down `llm/onecat-vllm-hy3-sm70`, then use uniquely labeled `FORGE-001` CUDA-devel jobs on the isolated eight-V100 node to compile the three candidate commits, run exact occupied-64K B1/B2 numerics and progressive microbenchmarks, collect direct q3 profiler evidence, and rank the portfolio. Build/publish an immutable image and run the isolated service canary only if all preceding gates pass, then restore and known-answer-check the captured deployment.
- Retry or idempotency information: Before any mutation, rerun Forge `check`, verify the three full commit hashes and clean worktrees, re-read the live deployment generation/image/argv/env/selectors, capture its exact spec and known answer, and probe that no `forge-001-<source-sha>-<config-hash>` resource or result directory already exists. Use only that derived identifier, never overwrite scored artifacts, delete only exact Forge-labeled resources, and restore only the matching snapshot. No prior mutating cluster action needs replay or cleanup.
- Plan remains valid: yes — live state still matches the recorded TP8/65,536/MTP2 target and the node remains fully reserved by the live service; local evidence strengthened the dispatch-cliff thesis but cannot rank the one-scan versus higher-CTA variants.
- Escalation review: clear — no scope, model, context, topology, quality, risk, or write-set change is required. The next boundary is the separately anticipated disruption/publication authority, not a plan amendment.
- Campaign envelope accounting: High campaign; three substantive rounds and thirteen concrete missions completed across segmented Triton, one-pass native Flash/WMMA, TurboQuant KV planning, production dispatch/fallback, CUDA-graph/MTP scheduling, speculation-depth, and topology families. Each round included independent falsification; at most the coordinator plus three workers ran concurrently. Static red teams repaired selectable-route false positives, stale timed-output validation, source-provenance ambiguity, q6 scratch capacity, native sequence-length OOB risk, and missing-extension false green. The twelve-mission/five-family/three-round floor is exceeded and the envelope is renewed for V100 discrimination. No explicit hard ceiling exists; the ungranted live-service isolation authority is the current external boundary.
- Progress commits since prior checkpoint: `9d4b1078eee26633bf35dd0426bdcc58cc67cf69` (`FORGE-001: checkpoint start Hy3 64K campaign`, canonical item/ledger); `0c952141360b2014b69139a126bd14b1af77accd` (`FORGE-001: add SM70 MTP segmented attention candidate`, the five segmented implementation/test/benchmark paths, local hooks and CPU oracles passed); `2f6c911cb5107117557d5bbe42d6ac67935e508f` plus `351f1bf9591f63921ce2cb4b54dd00aa8e49f686` (TurboQuant 64K planning and integration hardening, five owned paths, tests/hooks passed); `842ff44220bdde333c26ff51f7307d756c1c9537` (`FORGE-001: checkpoint add standalone q3 D128 XQA kernel`, five owned paths, static/falsification checks passed and SM70 execution explicitly pending).

#### Approach registry

| Family | Thesis | Artifact | Evidence | Exact gap | Status | Next discriminator | Reopen condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Segmented Triton M16/S16 | Restore at least 32 B1 CTAs with narrow q=2-4 dispatch and proven fallback | `0c952141360b2014b69139a126bd14b1af77accd` | Local planner, scratch, causal, paged, graph, provenance, and FP32-oracle checks pass | No SM70 compile/runtime, profiler, or integrated service result | candidate | Exact-64K B1/B2 V100 run and route/profile assertion | Reopen implementation on a numeric, graph, spill, or utilization failure |
| Segmented Triton M32/S32/S64 | Fuse all 24 q3 rows so each segment scans KV once | Benchmark-only routes in `0c952141360b2014b69139a126bd14b1af77accd` | Fail-closed harness makes nonselectable routes nonpromotable; one-scan geometry is explicit | Not production-selectable and unmeasured; lower-CTA variants may underfill SM70 | active | Progressive exact-64K comparison against M16/S16 | Integrate only if a valid route beats M16 beyond stability noise |
| Native q3/D128/GQA8 WMMA | One CTA processes all 24 rows per 512/1,024-token partition, loading each K/V tile once | `842ff44220bdde333c26ff51f7307d756c1c9537` | Two source audits found no remaining static WMMA/layout/shared/workspace/causal defect after capacity repairs | CUDA 12 SM70 build, PTXAS spills/registers, V100 numerics, graph content mutation, and performance absent | candidate | Build, compute-sanitizer, Nsight MMA/DRAM, exact-64K P512/P1024 B1/B2 versus Triton M32 | Production export/backend integration only after it wins the isolated discriminator |
| TurboQuant 64K KV | Four-times-smaller KV may raise capacity and reduce attention traffic if split planning and q3 graphs are safe | `2f6c911cb5107117557d5bbe42d6ac67935e508f` + `351f1bf9591f63921ce2cb4b54dd00aa8e49f686` | 64 splits cover 65,536; fixed workspace and configured-call propagation tested; q>1 capture fails closed | Global 64 splits may regress short/non-Flash paths; speculative q3 graph metadata is not graph-stable | blocked | V100 q1/quality/short-context A/B, then a separate graph-stable fused q3 packed-cache design if justified | Reopen q3 only with graph-stable metadata and quality-preserving fused GQA8 evidence |
| Existing scalar Flash-V100 small-query | Reuse existing small-q native path without a new grouped kernel | Existing source and Round 1/2 audit | Per-query/head KV rereads cannot realize the one-scan q3 thesis; broad scalar sweep is prohibited | No new reuse mechanism | redundant | Compare only as a correctness/performance control | Reopen only with concrete cross-query/head KV reuse |
| Speculation depth and graph/launch integration | MTP1 may win at 64K if q3 attention cost still exceeds added accepted tokens; MTP2 should win once fusion removes the cliff | Matched MTP1/MTP2 discriminator specification and retained short-context artifacts | Retained 13.2286/25.8151 short MTP1 is not an occupied-64K result; source audit found no safe ranking without hardware | No matched exact-64K run after attention repair | active | Alternate MTP1 and MTP2 at exact occupied 64K after kernel warmup | Reopen MTP4+ only with a new acceptance mechanism |
| TP4×PP2 topology | Reduce per-stage layer work through pipeline parallelism | Retained historical branch/results and Round 2 source audit | Slower retained results and per-step host materialization invalidate it as a current kernel discriminator | Host materialization remains unrepaired | falsified | None in this item | Reopen only after eliminating the measured per-step host path |

## Outcome

### Actual changes

Pending execution.

### Validation evidence

Pending execution; include the criterion-to-evidence trace and the adversarial audit result.

### Deviations

Pending execution.

### Work commit

- Progress commits: -
- Work commit: -

### Residual risk

Pending execution.

## Continuity

- New decisions: Pending outcome.
- Superseded decisions: Pending outcome.
- Unresolved gaps: Pending outcome.
- Follow-ups: Pending outcome.
- What later items must inherit: Pending outcome.
